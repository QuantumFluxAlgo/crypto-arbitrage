apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: executor-binding
subjects:
  - kind: ServiceAccount
    name: executor
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: executor-role
