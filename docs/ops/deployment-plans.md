# Deployment Plans (Markdown)

The original deployment guides were stored in DOCX format. They have been converted to Markdown for easier review and version tracking.

## Phase 1 – Local Setup
See `phase-1-local.md` for preparing a development environment and running tests.

## Phase 2 – Server Deployment
See `Phase-2-server-deployment.md` for provisioning the server and launching the stack.

## Phase 3 – Post Deploy Tests
See `Phase-3-post-deploy-tests.md` for final validation steps after go live.
